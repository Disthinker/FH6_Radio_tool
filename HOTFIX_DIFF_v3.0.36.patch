diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/README.md FH6_Radio_Tool_v3_0_36_automation_fallback_builder/README.md
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/README.md	2026-05-23 15:16:00.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/README.md	2026-05-23 16:07:13.908561856 +0000
@@ -222,7 +222,7 @@
 - This is intended to support stations whose playable songs are split across multiple bank files, while avoiding accidental matches from unrelated stations.
 
 
-## v3.0.29
+## v3.0.36
 
 - Added the official application icon to the PyInstaller EXE build.
 - The main window also uses the bundled icon when running from source or from the portable EXE package.
@@ -242,10 +242,10 @@
 - The Nuitka builder now treats `pywinauto`/Win32 automation dependencies as required and includes the relevant packages more aggressively.
 - The Nexus Nuitka package no longer bundles a separate `tools/ffmpeg.exe` by default, reducing quarantine risk.
 
-## v3.0.35 hotfix
+## v3.0.36 hotfix
 
 - Removed the obsolete user-facing **Auto-control Fmod Bank Tools** checkbox from the normal workflow.
 - One-click replacement, package generation, and main-menu music replacement now force Fmod Bank Tools automation internally.
 - Fixed the legacy path where unchecking the old option could still show a misleading `setup_env.bat` / `pywinauto` prompt in the portable EXE.
-- Portable EXE users should not use old v2 `setup_env.bat` files to repair the compiled program; update to v3.0.35 or newer instead.
+- Portable EXE users should not use old v2 `setup_env.bat` files to repair the compiled program; update to v3.0.36 or newer instead.
 
diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/RELEASE_NOTES.md FH6_Radio_Tool_v3_0_36_automation_fallback_builder/RELEASE_NOTES.md
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/RELEASE_NOTES.md	2026-05-23 15:15:29.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/RELEASE_NOTES.md	2026-05-23 16:07:15.820327588 +0000
@@ -1,7 +1,27 @@
-# FH6 Radio Tool v3.0.35 Hotfix
+# FH6 Radio Tool v3.0.36 Hotfix
+
+## Fixes
+
+- Fixed the Windows developer/VM error `ImportError: Typelib different than module` from pywinauto/comtypes.
+- One-click replacement no longer treats this pywinauto/comtypes import error as a missing environment.
+- Added an internal ctypes/Win32 fallback trigger for Fmod Bank Tools Extract/Rebuild. This fallback does not require pywinauto or comtypes.
+- Added best-effort cleanup/retry for stale comtypes generated typelib cache.
+- PyInstaller packaging is now the recommended build path when Nuitka behaves inconsistently on Windows VMs.
+
+## Recommended build
+
+Run `build_pyinstaller_release.bat` on native Windows. It creates:
+
+`dist_release\FH6_Radio_Tool_v3.0.36_nexus_exe_portable.zip`
+
+Nuitka scripts are still kept, but PyInstaller is the safer packaging option for this release.
+
+---
+
+# FH6 Radio Tool v3.0.36 Hotfix
 
 - Removed the obsolete user-facing **Auto-control Fmod Bank Tools** checkbox from the normal workflow.
 - One-click replacement, package generation, and main-menu music replacement now force Fmod Bank Tools GUI automation internally.
 - Fixed the legacy path where unchecking the old option could still show a misleading `setup_env.bat` / `pywinauto` prompt in the portable EXE.
-- If automation dependencies are missing in a compiled EXE, the tool now reports it as a bad/old build and asks for v3.0.35 or newer, instead of asking users to run old v2 setup scripts.
+- If automation dependencies are missing in a compiled EXE, the tool now reports it as a bad/old build and asks for v3.0.36 or newer, instead of asking users to run old v2 setup scripts.
 - Main-menu music replacement remains fixed to `GLB_RadioPressStart.assets.bank`; users only choose the replacement music file.
diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/build_nuitka_release.bat FH6_Radio_Tool_v3_0_36_automation_fallback_builder/build_nuitka_release.bat
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/build_nuitka_release.bat	2026-05-23 15:09:04.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/build_nuitka_release.bat	2026-05-23 16:09:13.603415204 +0000
@@ -62,6 +62,7 @@
   echo   3. Close any running FH6RadioTool.exe.
   echo   4. If antivirus blocks the build, add the project folder to trusted locations.
   echo   5. Make sure Microsoft C++ Build Tools or a supported compiler is installed.
+  echo   6. If Nuitka remains unstable in your VM, run build_pyinstaller_release.bat instead.
   pause
   exit /b 1
 )
diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/build_portable_release.bat FH6_Radio_Tool_v3_0_36_automation_fallback_builder/build_portable_release.bat
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/build_portable_release.bat	2026-05-23 13:08:54.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/build_portable_release.bat	2026-05-23 16:07:40.948868192 +0000
@@ -3,14 +3,14 @@
 setlocal EnableExtensions
 cd /d "%~dp0"
 
-set "APP_VERSION=3.0.29"
+set "APP_VERSION=3.0.36"
 
 echo.
 echo ============================================================
-echo  FH6 Radio Tool v%APP_VERSION% - Nexus-safe EXE Portable Release Builder
+echo  FH6 Radio Tool v%APP_VERSION% - PyInstaller EXE Portable Release Builder
 echo ============================================================
 echo.
-echo This script builds a Nexus-safe Windows portable EXE package.
+echo This script builds a Windows portable EXE package using PyInstaller. For v3.0.36, build_pyinstaller_release.bat is the recommended alias.
 echo Output:
 echo   dist_release\FH6_Radio_Tool_v%APP_VERSION%_nexus_exe_portable.zip
 echo.
diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/build_pyinstaller_release.bat FH6_Radio_Tool_v3_0_36_automation_fallback_builder/build_pyinstaller_release.bat
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/build_pyinstaller_release.bat	1970-01-01 00:00:00.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/build_pyinstaller_release.bat	2026-05-23 16:07:39.229937937 +0000
@@ -0,0 +1,75 @@
+@echo off
+chcp 65001 >nul
+setlocal EnableExtensions
+cd /d "%~dp0"
+
+echo.
+echo ============================================================
+echo  FH6 Radio Tool - Recommended PyInstaller Release Builder
+echo ============================================================
+echo.
+echo Output:
+echo   dist_release\FH6_Radio_Tool_v*_nexus_exe_portable.zip
+echo.
+echo Notes:
+echo   - This is the recommended builder for v3.0.36.
+echo   - Run this on native Windows, not WSL/Linux.
+echo   - Fmod Bank Tools is NOT bundled. Players select their own exe.
+echo   - The final ZIP contains no loose .ico, source folder, or nested archive.
+echo.
+
+if not exist "fh6_radio_tool\app.py" (
+  echo [ERROR] Please run this script from the FH6 Radio Tool project root.
+  pause
+  exit /b 1
+)
+
+set "PY=.venv\Scripts\python.exe"
+if not exist "%PY%" (
+  echo [1/5] .venv not found. Running setup_env.bat first ...
+  call setup_env.bat
+  if errorlevel 1 (
+    echo [ERROR] setup_env.bat failed.
+    pause
+    exit /b 1
+  )
+)
+
+if not exist "%PY%" (
+  echo [ERROR] Python venv still not found: %PY%
+  pause
+  exit /b 1
+)
+
+echo [2/5] Installing / updating PyInstaller build dependencies ...
+"%PY%" -m pip install -U pip
+"%PY%" -m pip install -U -r requirements.txt
+"%PY%" -m pip install -U pyinstaller pyinstaller-hooks-contrib pywinauto pywin32 comtypes six
+if errorlevel 1 (
+  echo [ERROR] Failed to install build dependencies.
+  pause
+  exit /b 1
+)
+
+echo [3/5] Building PyInstaller EXE portable package ...
+"%PY%" scripts\build_exe_release.py
+if errorlevel 1 (
+  echo.
+  echo [ERROR] PyInstaller package build failed.
+  echo Common fixes:
+  echo   1. Close any running FH6RadioTool.exe.
+  echo   2. Put the project in a short local path, e.g. C:\FH6Build.
+  echo   3. If antivirus blocks the build, add the project folder to trusted locations.
+  echo   4. If pywinauto reports Typelib different than module, delete .venv and run this script again.
+  pause
+  exit /b 1
+)
+
+echo [4/5] Build completed.
+echo.
+echo [5/5] Result:
+echo   dist_release\FH6_Radio_Tool_v*_nexus_exe_portable.zip
+echo.
+echo Extract that ZIP and double-click FH6RadioTool.exe to test.
+echo.
+pause
diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/docs/User_Guide_EN.md FH6_Radio_Tool_v3_0_36_automation_fallback_builder/docs/User_Guide_EN.md
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/docs/User_Guide_EN.md	2026-05-23 15:15:29.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/docs/User_Guide_EN.md	2026-05-23 16:07:13.909567082 +0000
@@ -149,7 +149,7 @@
 
 FH6 Radio Tool does not include Fmod Bank Tools. Select its `Fmod_Bank_Tools.exe` path in the final step.
 
-During Extract/Rebuild, do not close the Fmod Bank Tools window manually. Starting from v3.0.35, normal users no longer need an Auto-control checkbox; the portable EXE enables the workflow automatically. If the automation component is still reported unavailable, update to v3.0.35 or newer.
+During Extract/Rebuild, do not close the Fmod Bank Tools window manually. Starting from v3.0.36, normal users no longer need an Auto-control checkbox; the portable EXE enables the workflow automatically. If the automation component is still reported unavailable, update to v3.0.36 or newer.
 
 ## 8. Troubleshooting
 
diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/docs/User_Guide_ZH.md FH6_Radio_Tool_v3_0_36_automation_fallback_builder/docs/User_Guide_ZH.md
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/docs/User_Guide_ZH.md	2026-05-23 15:15:29.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/docs/User_Guide_ZH.md	2026-05-23 16:07:13.910215269 +0000
@@ -149,7 +149,7 @@
 
 FH6 Radio Tool 不直接包含 Fmod Bank Tools。你需要在工具里选择它的 `Fmod_Bank_Tools.exe`。
 
-如果一键流程中 Fmod Bank Tools 弹出窗口，请不要在 Extract/Rebuild 过程中手动关闭它。v3.0.35 起普通用户不再需要勾选自动控制选项；便携 EXE 会自动启用该流程。若仍提示自动控制组件不可用，请更新到 v3.0.35 或更新版本。
+如果一键流程中 Fmod Bank Tools 弹出窗口，请不要在 Extract/Rebuild 过程中手动关闭它。v3.0.36 起普通用户不再需要勾选自动控制选项；便携 EXE 会自动启用该流程。若仍提示自动控制组件不可用，请更新到 v3.0.36 或更新版本。
 
 ## 8. 常见问题
 
diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/fh6_radio_tool/__init__.py FH6_Radio_Tool_v3_0_36_automation_fallback_builder/fh6_radio_tool/__init__.py
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/fh6_radio_tool/__init__.py	2026-05-23 15:15:29.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/fh6_radio_tool/__init__.py	2026-05-23 16:07:13.907157466 +0000
@@ -1 +1 @@
-__version__ = "3.0.35"
+__version__ = "3.0.36"
diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/fh6_radio_tool/fmod_automation.py FH6_Radio_Tool_v3_0_36_automation_fallback_builder/fh6_radio_tool/fmod_automation.py
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/fh6_radio_tool/fmod_automation.py	2026-05-23 15:15:29.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/fh6_radio_tool/fmod_automation.py	2026-05-23 16:08:48.792128341 +0000
@@ -4,8 +4,8 @@
 
 FH6 Radio Tool does not bundle or link Fmod Bank Tools.  This module only
 prepares the external tool's working directories, writes its config.ini, launches
-its executable, and optionally tries GUI automation through pywinauto if the user
-has installed it locally.
+its executable, and tries to trigger GUI actions.  pywinauto is used when it is
+healthy; a built-in Win32 fallback is used when pywinauto/comtypes is broken.
 """
 
 import json
@@ -60,28 +60,145 @@
 
 
 
+_COMTYPES_REPAIR_ATTEMPTED = False
+
+
+def _automation_win32_fallback_available() -> bool:
+    """Return whether the built-in ctypes/Win32 GUI trigger can be used.
+
+    v3.0.36: pywinauto can fail in otherwise valid Windows environments with
+    errors such as "Typelib different than module" from a stale comtypes cache.
+    The one-click workflow should not be blocked by that import error, because
+    Fmod Bank Tools can also be triggered with normal Win32 window messages and
+    Alt-key shortcuts.
+    """
+    return os.name == "nt"
+
+
+def _purge_partial_automation_modules() -> None:
+    """Drop partially imported pywinauto/comtypes generated modules before retry."""
+    import sys
+
+    for name in list(sys.modules):
+        if name == "pywinauto" or name.startswith("pywinauto.") or name.startswith("comtypes.gen."):
+            sys.modules.pop(name, None)
+
+
+def _clear_comtypes_generated_cache() -> list[str]:
+    """Best-effort cleanup for stale comtypes generated typelib modules.
+
+    The common Windows failure is:
+        ImportError: Typelib different than module
+    It is usually caused by old files in comtypes.gen / comtypes_cache.  Clearing
+    generated files is safe: comtypes regenerates them when needed.  This helper
+    never raises; it returns a short list of touched paths for diagnostics.
+    """
+    global _COMTYPES_REPAIR_ATTEMPTED
+    if _COMTYPES_REPAIR_ATTEMPTED:
+        return []
+    _COMTYPES_REPAIR_ATTEMPTED = True
+
+    touched: list[str] = []
+    candidates: list[Path] = []
+    try:
+        import tempfile
+        candidates.append(Path(tempfile.gettempdir()) / "comtypes_cache")
+    except Exception:
+        pass
+    try:
+        local = os.environ.get("LOCALAPPDATA")
+        if local:
+            candidates.append(Path(local) / "Temp" / "comtypes_cache")
+    except Exception:
+        pass
+    try:
+        import comtypes.gen  # type: ignore
+        for item in getattr(comtypes.gen, "__path__", []) or []:
+            candidates.append(Path(item))
+    except Exception:
+        pass
+
+    seen: set[Path] = set()
+    for cand in candidates:
+        try:
+            cand = cand.resolve()
+        except Exception:
+            continue
+        if cand in seen or not cand.exists() or not cand.is_dir():
+            continue
+        seen.add(cand)
+        name = cand.name.lower()
+        if name not in {"gen", "comtypes_cache"}:
+            continue
+        try:
+            for child in cand.iterdir():
+                if child.name == "__init__.py":
+                    continue
+                if child.is_dir():
+                    shutil.rmtree(child, ignore_errors=True)
+                    touched.append(str(child))
+                elif child.suffix.lower() in {".py", ".pyc", ".pyo"}:
+                    child.unlink(missing_ok=True)
+                    touched.append(str(child))
+        except Exception:
+            continue
+    return touched
+
+
+def _import_pywinauto_for_trigger():
+    """Import pywinauto trigger pieces, repairing stale comtypes cache once."""
+    try:
+        with warnings.catch_warnings():
+            warnings.simplefilter("ignore")
+            from pywinauto import Application  # type: ignore
+            from pywinauto.keyboard import send_keys  # type: ignore
+        return Application, send_keys, ""
+    except Exception as first_exc:
+        msg = f"{type(first_exc).__name__}: {first_exc}"
+        if "typelib" in msg.lower() or "comtypes" in msg.lower():
+            cleared = _clear_comtypes_generated_cache()
+            _purge_partial_automation_modules()
+            try:
+                with warnings.catch_warnings():
+                    warnings.simplefilter("ignore")
+                    from pywinauto import Application  # type: ignore
+                    from pywinauto.keyboard import send_keys  # type: ignore
+                suffix = f"; repaired comtypes cache entries={len(cleared)}" if cleared else "; retried after comtypes cache check"
+                return Application, send_keys, suffix
+            except Exception as second_exc:
+                return None, None, msg + f"; retry failed: {type(second_exc).__name__}: {second_exc}"
+        return None, None, msg
+
+
 def pywinauto_status() -> tuple[bool, str]:
-    """Return whether pywinauto GUI automation is importable, plus detail for diagnostics.
+    """Return whether automatic GUI control is available, plus diagnostics.
 
-    v3.0.35: packaged EXE builds must carry this dependency inside the EXE.
-    The normal user workflow no longer exposes an Auto-control checkbox; telling
-    end users to run setup_env.bat is wrong for the Nexus/Nuitka portable package
-    because the final ZIP intentionally does not include batch installers.
+    Kept under the old function name for compatibility with the UI code.  Since
+    v3.0.36, a broken pywinauto/comtypes import no longer means the whole one-
+    click workflow is unavailable on Windows: the tool can fall back to built-in
+    ctypes/Win32 button/shortcut triggering.
     """
     try:
         with warnings.catch_warnings():
             warnings.simplefilter("ignore")
             import pywinauto  # type: ignore
-            from pywinauto import Application  # type: ignore  # noqa: F401
-            from pywinauto.keyboard import send_keys  # type: ignore  # noqa: F401
-        version = getattr(pywinauto, "__version__", "unknown")
-        return True, f"pywinauto OK, version={version}"
-    except Exception as exc:
-        return False, f"{type(exc).__name__}: {exc}"
+        Application, send_keys, repair_msg = _import_pywinauto_for_trigger()
+        if Application and send_keys:
+            version = getattr(pywinauto, "__version__", "unknown")
+            return True, f"pywinauto OK, version={version}{repair_msg}"
+    except Exception:
+        pass
+
+    Application, send_keys, detail = _import_pywinauto_for_trigger()
+    if Application and send_keys:
+        return True, f"pywinauto OK{detail}"
+    if _automation_win32_fallback_available():
+        return True, f"pywinauto unavailable ({detail}); using built-in Win32 fallback"
+    return False, detail or "pywinauto unavailable and Win32 fallback is not available on this platform"
 
 
 def is_pywinauto_available() -> bool:
-    """Return whether optional pywinauto GUI automation is importable."""
+    """Return whether automatic Fmod GUI triggering is available."""
     return pywinauto_status()[0]
 
 
@@ -92,14 +209,12 @@
     if is_frozen_app():
         return (
             f"自动控制组件不可用：{detail}。"
-            "当前是便携 EXE/Nuitka 包，包内不包含 setup_env.bat；"
-            "运行旧版 v2 的 setup_env.bat 也不会修改这个 EXE。"
-            "请改用 v3.0.35 或更新版本重新打包/下载。"
+            "请使用 v3.0.36 或更新版本重新下载/重新打包；不要运行旧版 v2 的 setup_env.bat 修复 EXE。"
         )
     return (
         f"自动控制组件不可用：{detail}。"
-        "这是开发者源码运行环境的依赖缺失；请在当前 v3 源码环境中安装 requirements.txt，"
-        "不要复用 v2 旧环境。"
+        "如果是在开发者源码环境中运行，请先执行 setup_env.bat 或 pip install -r requirements.txt；"
+        "如果出现 Typelib different than module，请删除旧虚拟环境后用 v3.0.36 重新安装，或直接使用 PyInstaller 打包版。"
     )
 
 def layout_from_exe(exe_path: Path) -> FmodToolLayout:
@@ -539,52 +654,185 @@
     )
 
 
-def try_trigger_gui_action(pid: int, action: str, timeout_sec: int = 12) -> tuple[bool, str]:
-    """Best-effort GUI automation.  Requires optional pywinauto.
+def _find_main_window_by_pid(pid: int) -> int | None:
+    if os.name != "nt":
+        return None
+    try:
+        import ctypes
+        from ctypes import wintypes
+    except Exception:
+        return None
+
+    user32 = ctypes.windll.user32
+    hwnds: list[int] = []
 
-    The upstream Fmod Bank Tools currently exposes Extract/Rebuild primarily as
-    GUI actions, not a documented CLI.  This function is deliberately optional:
-    failure should not block the workflow because the tool directories and
-    config.ini are already prepared by this module.
+    EnumWindowsProc = ctypes.WINFUNCTYPE(wintypes.BOOL, wintypes.HWND, wintypes.LPARAM)
+
+    def callback(hwnd, lparam):
+        try:
+            if not user32.IsWindowVisible(hwnd):
+                return True
+            proc_id = wintypes.DWORD()
+            user32.GetWindowThreadProcessId(hwnd, ctypes.byref(proc_id))
+            if proc_id.value == pid:
+                hwnds.append(int(hwnd))
+        except Exception:
+            pass
+        return True
+
+    try:
+        user32.EnumWindows(EnumWindowsProc(callback), 0)
+    except Exception:
+        return None
+    return hwnds[0] if hwnds else None
+
+
+def _window_text(hwnd: int) -> str:
+    try:
+        import ctypes
+        user32 = ctypes.windll.user32
+        length = user32.GetWindowTextLengthW(hwnd)
+        if length <= 0:
+            return ""
+        buf = ctypes.create_unicode_buffer(length + 1)
+        user32.GetWindowTextW(hwnd, buf, length + 1)
+        return buf.value
+    except Exception:
+        return ""
+
+
+def _class_name(hwnd: int) -> str:
+    try:
+        import ctypes
+        user32 = ctypes.windll.user32
+        buf = ctypes.create_unicode_buffer(256)
+        user32.GetClassNameW(hwnd, buf, 256)
+        return buf.value
+    except Exception:
+        return ""
+
+
+def _trigger_gui_action_win32(pid: int, action: str, timeout_sec: int = 12) -> tuple[bool, str]:
+    """Trigger Extract/Rebuild using only built-in ctypes Win32 APIs.
+
+    This path avoids pywinauto and comtypes entirely.  It first tries to find a
+    visible child button/menu item containing the action text and sends BM_CLICK.
+    If that is not possible, it focuses the main window and sends Alt+E/Alt+R.
+    The caller still waits for real output files, so a false positive here will
+    be caught by the normal timeout/output verification logic.
     """
+    if os.name != "nt":
+        return False, "Win32 fallback is only available on Windows."
     action_lower = action.lower().strip()
     if action_lower not in {"extract", "rebuild"}:
         return False, f"未知 action: {action}"
     try:
-        with warnings.catch_warnings():
-            warnings.simplefilter("ignore")
-            from pywinauto import Application  # type: ignore
-            from pywinauto.keyboard import send_keys  # type: ignore
-    except Exception:
-        return False, pywinauto_user_hint() + " 已启动 Fmod Bank Tools，请在其界面手动点击 Extract/Rebuild。"
+        import ctypes
+        from ctypes import wintypes
+    except Exception as exc:
+        return False, f"Win32 fallback unavailable: {type(exc).__name__}: {exc}"
+
+    user32 = ctypes.windll.user32
+    BM_CLICK = 0x00F5
+    SW_RESTORE = 9
+    VK_MENU = 0x12
+    VK_E = 0x45
+    VK_R = 0x52
+    KEYEVENTF_KEYUP = 0x0002
 
     deadline = time.time() + timeout_sec
     last_error = ""
     while time.time() < deadline:
+        hwnd = _find_main_window_by_pid(pid)
+        if not hwnd:
+            last_error = "main window not found"
+            time.sleep(0.25)
+            continue
         try:
-            app = Application(backend="uia").connect(process=pid)
-            win = app.top_window()
-            win.set_focus()
-            # Try menu/button text first; UI labels differ by release, so keep this fuzzy.
-            for ctrl in win.descendants():
-                try:
-                    text = (ctrl.window_text() or "").strip().lower()
-                    cls = (ctrl.friendly_class_name() or "").lower()
-                    if action_lower in text and cls in {"button", "menuitem", "splitbutton"}:
-                        ctrl.click_input()
-                        return True, f"已通过 pywinauto 点击 {action} 控件。"
-                except Exception:
-                    continue
-            # Fallback: try Alt menu mnemonics if the app has simple menu actions.
-            if action_lower == "extract":
-                send_keys("%e")
-            else:
-                send_keys("%r")
-            return True, f"已发送 {action} 快捷键尝试触发操作。"
+            user32.ShowWindow(hwnd, SW_RESTORE)
+            user32.SetForegroundWindow(hwnd)
+        except Exception:
+            pass
+
+        clicked: list[str] = []
+        EnumChildProc = ctypes.WINFUNCTYPE(wintypes.BOOL, wintypes.HWND, wintypes.LPARAM)
+
+        def child_callback(child_hwnd, lparam):
+            if clicked:
+                return False
+            try:
+                text = _window_text(int(child_hwnd)).strip().lower()
+                cls = _class_name(int(child_hwnd)).strip().lower()
+                if action_lower in text and ("button" in cls or "menu" in cls or text == action_lower):
+                    user32.SendMessageW(child_hwnd, BM_CLICK, 0, 0)
+                    clicked.append(text or cls or str(int(child_hwnd)))
+                    return False
+            except Exception:
+                pass
+            return True
+
+        try:
+            user32.EnumChildWindows(hwnd, EnumChildProc(child_callback), 0)
+        except Exception as exc:
+            last_error = f"EnumChildWindows failed: {exc}"
+        if clicked:
+            return True, f"已通过内置 Win32 fallback 点击 {action} 控件：{clicked[0]}。"
+
+        try:
+            key = VK_E if action_lower == "extract" else VK_R
+            user32.keybd_event(VK_MENU, 0, 0, 0)
+            user32.keybd_event(key, 0, 0, 0)
+            user32.keybd_event(key, 0, KEYEVENTF_KEYUP, 0)
+            user32.keybd_event(VK_MENU, 0, KEYEVENTF_KEYUP, 0)
+            return True, f"已通过内置 Win32 fallback 发送 {action} 快捷键。"
         except Exception as exc:
             last_error = str(exc)
-            time.sleep(0.5)
-    return False, f"未能自动触发 {action}：{last_error or '窗口未就绪'}；请手动点击。"
+            time.sleep(0.25)
+    return False, f"内置 Win32 fallback 未能触发 {action}：{last_error or '窗口未就绪'}。"
+
+
+def try_trigger_gui_action(pid: int, action: str, timeout_sec: int = 12) -> tuple[bool, str]:
+    """Best-effort GUI automation with pywinauto plus a Win32 fallback."""
+    action_lower = action.lower().strip()
+    if action_lower not in {"extract", "rebuild"}:
+        return False, f"未知 action: {action}"
+
+    Application, send_keys, import_detail = _import_pywinauto_for_trigger()
+    if Application and send_keys:
+        deadline = time.time() + timeout_sec
+        last_error = ""
+        while time.time() < deadline:
+            try:
+                app = Application(backend="win32").connect(process=pid)
+                win = app.top_window()
+                win.set_focus()
+                # Try menu/button text first; UI labels differ by release, so keep this fuzzy.
+                for ctrl in win.descendants():
+                    try:
+                        text = (ctrl.window_text() or "").strip().lower()
+                        cls = (ctrl.friendly_class_name() or "").lower()
+                        if action_lower in text and cls in {"button", "menuitem", "splitbutton"}:
+                            ctrl.click_input()
+                            return True, f"已通过 pywinauto(win32) 点击 {action} 控件。"
+                    except Exception:
+                        continue
+                if action_lower == "extract":
+                    send_keys("%e")
+                else:
+                    send_keys("%r")
+                return True, f"已通过 pywinauto(win32) 发送 {action} 快捷键尝试触发操作。"
+            except Exception as exc:
+                last_error = str(exc)
+                time.sleep(0.5)
+        win32_ok, win32_msg = _trigger_gui_action_win32(pid, action, timeout_sec=max(3, min(timeout_sec, 12)))
+        if win32_ok:
+            return True, win32_msg + f" pywinauto 未成功：{last_error or import_detail}"
+        return False, f"未能自动触发 {action}：pywinauto={last_error or import_detail}; win32={win32_msg}；请手动点击。"
+
+    win32_ok, win32_msg = _trigger_gui_action_win32(pid, action, timeout_sec=timeout_sec)
+    if win32_ok:
+        return True, win32_msg + f" pywinauto 不可用：{import_detail}"
+    return False, f"未能自动触发 {action}：pywinauto={import_detail}; win32={win32_msg}；请手动点击。"
 
 
 def write_manifest(path: Path, action: str, layout: FmodToolLayout, payload: dict) -> Path:
@@ -1082,7 +1330,7 @@
             return FmodAutomationResult(
                 False,
                 action,
-                message + " 内部自动触发未启用，因此已停止等待；请使用 v3.0.35 或更新版本重新构建。" + pywinauto_user_hint(),
+                message + " 内部自动触发未启用，因此已停止等待；请使用 v3.0.36 或更新版本重新构建。" + pywinauto_user_hint(),
                 layout,
                 None,
                 proc.pid,
diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/fh6_radio_tool/v2_state_store.py FH6_Radio_Tool_v3_0_36_automation_fallback_builder/fh6_radio_tool/v2_state_store.py
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/fh6_radio_tool/v2_state_store.py	2026-05-23 15:15:29.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/fh6_radio_tool/v2_state_store.py	2026-05-23 16:07:13.907984286 +0000
@@ -9,7 +9,7 @@
 
 from .runtime_tools import runtime_root
 
-APP_VERSION = "3.0.35"
+APP_VERSION = "3.0.36"
 DB_FILE_NAME = "fh6_radio_tool_v2.sqlite3"
 
 
diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/fh6_radio_tool/v2_ui.py FH6_Radio_Tool_v3_0_36_automation_fallback_builder/fh6_radio_tool/v2_ui.py
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/fh6_radio_tool/v2_ui.py	2026-05-23 15:15:09.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/fh6_radio_tool/v2_ui.py	2026-05-23 16:08:16.526555731 +0000
@@ -368,7 +368,7 @@
         self.bank_root_edit.setReadOnly(True)
         self.bank_root_edit.setPlaceholderText("扫描游戏根目录后自动定位 media/Audio/FMODBanks")
         self.fmod_tool_edit = QLineEdit(str(self.store.get_setting("fmod_tool", "")))
-        # v3.0.35: one-click / package workflows always use automatic Fmod Bank Tools
+        # v3.0.36: one-click / package workflows always use automatic Fmod Bank Tools
         # control.  The old user-toggle caused a confusing legacy path where unchecking
         # it made the app ask for setup_env.bat/pywinauto even in the portable EXE.
         # Keep the QCheckBox object only for backward compatibility with saved UI code,
@@ -1364,7 +1364,7 @@
     def automation_component_warning_text(self, context_zh: str, context_en: str) -> tuple[str, str]:
         """User-facing message when pywinauto is unavailable.
 
-        v3.0.35 removes the obsolete auto-control checkbox and fixes the legacy path that asked Nexus EXE users
+        v3.0.36 removes the obsolete auto-control checkbox and fixes the legacy path that asked Nexus EXE users
         to run setup_env.bat even though the portable package intentionally does
         not contain setup_env.bat.
         """
@@ -1374,15 +1374,15 @@
         if is_frozen_app():
             zh = (
                 f"{context_zh}需要自动控制 Fmod Bank Tools。\n\n"
-                f"当前便携 EXE 内置的自动控制组件不可用：{detail}\n\n"
+                f"当前便携 EXE 的自动控制组件不可用：{detail}\n\n"
                 "这个 Nexus/便携 EXE 包不会包含 setup_env.bat；运行 v2 旧版 setup_env.bat 也不会修复当前 EXE。\n"
-                "请使用 v3.0.35 或更新版本重新下载/重新打包。"
+                "请使用 v3.0.36 或更新版本重新下载/重新打包；不要使用旧 v2 环境修复 EXE。"
             )
             en = (
                 f"{context_en} requires automatic control of Fmod Bank Tools.\n\n"
                 f"The automation component bundled in this portable EXE is not available: {detail}\n\n"
                 "This Nexus/portable EXE package does not include setup_env.bat, and running an old v2 setup_env.bat will not repair the current EXE.\n"
-                "Please use v3.0.35 or a newer rebuilt package."
+                "Please use v3.0.36 or a newer rebuilt package; do not repair this EXE with an old v2 environment."
             )
         else:
             zh = (
@@ -1400,7 +1400,7 @@
     def _fmod_auto_click_enabled(self) -> bool:
         """Return the forced Fmod GUI automation setting.
 
-        v3.0.35 removes the old user-facing auto-control checkbox from the
+        v3.0.36 removes the old user-facing auto-control checkbox from the
         normal workflow.  One-click and package generation require GUI
         automation, so allowing users to disable it only created a dead-end
         legacy warning about setup_env.bat/pywinauto.
diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/scripts/build_exe_release.py FH6_Radio_Tool_v3_0_36_automation_fallback_builder/scripts/build_exe_release.py
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/scripts/build_exe_release.py	2026-05-23 13:08:54.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/scripts/build_exe_release.py	2026-05-23 16:07:55.975746186 +0000
@@ -137,14 +137,14 @@
 1. 双击 FH6RadioTool.exe 启动工具，不需要先运行 setup_env.bat。
 2. Fmod Bank Tools 仍然不会内置；请在工具设置里选择你自己的 Fmod_Bank_Tools.exe。
 3. output、backup、work 会生成在 FH6RadioTool.exe 同目录。
-4. 如果安全软件拦截，请把本文件夹加入信任后重新解压。
+4. v3.0.36 已内置 Win32 fallback；如果 pywinauto/comtypes 异常，仍会尝试自动控制 Fmod Bank Tools。
 5. 如果 EXE 版异常，仍可回到源码包使用 setup_env.bat + run_tool.bat。
 
 English:
 1. Double-click FH6RadioTool.exe to start. Python setup is not required.
 2. Fmod Bank Tools is still external; select your own Fmod_Bank_Tools.exe in settings.
 3. output, backup and work are created next to FH6RadioTool.exe.
-4. If antivirus blocks the app, add this folder to trusted locations and extract again.
+4. v3.0.36 includes a Win32 fallback; if pywinauto/comtypes is broken, it can still try to control Fmod Bank Tools.
 5. If this EXE build has issues, use the source/batch package as fallback.
 """
     (bundle_dir / "README_EXE_PORTABLE.txt").write_text(text, encoding="utf-8")
@@ -262,7 +262,8 @@
     shutil.rmtree(stage_root, ignore_errors=True)
     package_dir.mkdir(parents=True, exist_ok=True)
     shutil.copy2(onefile_exe, package_dir / "FH6RadioTool.exe")
-    copy_ffmpeg_to_bundle(package_dir)
+    # Do not copy a loose tools/ffmpeg.exe into the Nexus upload package.
+    # imageio-ffmpeg is collected into the one-file EXE by PyInstaller.
     if (PROJECT_ROOT / "config").exists():
         shutil.copytree(PROJECT_ROOT / "config", package_dir / "config")
     # Do not copy loose .ico files into the final Nexus upload package.
diff -ruN /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/scripts/build_nuitka_release.py FH6_Radio_Tool_v3_0_36_automation_fallback_builder/scripts/build_nuitka_release.py
--- /mnt/data/fh6_fix_current/FH6_Radio_Tool_v3_0_35_auto_control_cleanup_builder/scripts/build_nuitka_release.py	2026-05-23 15:15:29.000000000 +0000
+++ FH6_Radio_Tool_v3_0_36_automation_fallback_builder/scripts/build_nuitka_release.py	2026-05-23 16:09:13.603043086 +0000
@@ -19,10 +19,9 @@
 APP_ICON_B64 = PROJECT_ROOT / "resources" / "app_icon_base64.txt"
 ARCHIVE_EXTENSIONS = {".zip", ".7z", ".rar", ".tar", ".gz", ".tgz", ".bz2", ".tbz", ".xz", ".cab", ".iso"}
 
-# v3.0.35: automatic Fmod Bank Tools control is a required feature for the
-# portable EXE.  Do not treat pywinauto as a vague optional import, otherwise a
-# build can succeed but users can hit a misleading legacy setup_env.bat prompt in a
-# Nexus package that intentionally has no setup_env.bat.
+# v3.0.36: automatic Fmod Bank Tools control can use either pywinauto
+# or the built-in ctypes/Win32 fallback.  Do not fail the whole build only
+# because pywinauto/comtypes has a stale typelib cache.
 REQUIRED_WINDOWS_AUTOMATION_IMPORTS = [
     "pywinauto",
     "pywinauto.application",
@@ -152,7 +151,7 @@
 3. output、backup、work 会生成在 FH6RadioTool.exe 同目录。
 4. 本包不包含 loose .ico、游戏文件、音乐文件、bank 文件或源码目录。
 5. 本 EXE 已内置自动控制依赖；用户不需要、也不能通过旧版 setup_env.bat 修复此 EXE。
-6. 如果自动控制仍提示缺失，请下载 v3.0.35 或更新版本重新打包。
+6. 如果自动控制仍提示缺失，请下载 v3.0.36 或更新版本重新打包。
 
 English:
 1. Double-click FH6RadioTool.exe to start. Python setup is not required.
@@ -160,7 +159,7 @@
 3. output, backup and work are created next to FH6RadioTool.exe.
 4. This package contains no loose .ico files, game files, music files, bank files or source folder.
 5. This EXE should include the automation dependencies; users do not need and cannot repair it with an old setup_env.bat.
-6. If automation is still reported missing, rebuild/download v3.0.35 or newer.
+6. If automation is still reported missing, rebuild/download v3.0.36 or newer.
 """
     (bundle_dir / "README_NUITKA_PORTABLE.txt").write_text(text, encoding="utf-8")
 
@@ -215,17 +214,19 @@
 
 
 def verify_windows_automation_imports() -> None:
-    missing: list[str] = []
-    for mod in REQUIRED_WINDOWS_AUTOMATION_IMPORTS:
-        if not importable(mod):
-            missing.append(mod)
-    if missing:
-        raise RuntimeError(
-            "Windows automation dependencies are missing: " + ", ".join(missing) +
-            ". Run build_nuitka_release.bat again from the v3.0.35 project root. "
-            "Do not reuse an old v2 setup_env.bat or an old virtualenv."
-        )
-    run([sys.executable, "-c", "from pywinauto import Application; from pywinauto.keyboard import send_keys; import pythoncom, pywintypes, win32gui; print('pywinauto runtime check OK')"])
+    """Check automation support without requiring a healthy pywinauto/comtypes import.
+
+    v3.0.36 can use an internal ctypes/Win32 fallback when pywinauto fails with
+    stale comtypes errors such as "Typelib different than module". Therefore the
+    build should not be blocked solely because pywinauto import is broken.
+    """
+    code = (
+        "from fh6_radio_tool.fmod_automation import pywinauto_status; "
+        "ok, detail = pywinauto_status(); "
+        "print('automation status:', ok, detail); "
+        "raise SystemExit(0 if ok else 1)"
+    )
+    run([sys.executable, "-c", code])
 
 
 def main() -> int:
@@ -313,7 +314,7 @@
     shutil.rmtree(stage_root, ignore_errors=True)
     package_dir.mkdir(parents=True, exist_ok=True)
     shutil.copy2(onefile_exe, package_dir / "FH6RadioTool.exe")
-    # v3.0.35: do not bundle a separate ffmpeg.exe in the Nexus package.
+    # v3.0.36: do not bundle a separate ffmpeg.exe in the Nexus package.
     # The app can use imageio-ffmpeg from the bundled Python package or a user-provided PATH/tool.
     if (PROJECT_ROOT / "config").exists():
         shutil.copytree(PROJECT_ROOT / "config", package_dir / "config")
