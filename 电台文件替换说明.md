# FH6 电台音频替换机制说明与操作指南

本文档用于说明 FH6 电台系统在音频替换中的基本运作方式，并总结如何更稳定地替换普通电台歌曲、主菜单音乐、Stinger/Jingle、VO_DJ 语音和部分音效。  
本文基于当前 FH6 Radio Tool 开发者模式扫描结果和实际替换测试经验整理，重点面向 **mod 制作与工具开发**。

---

## 0. 一句话总原则

**Bank 负责真正的声音，RadioInfo XML 负责普通歌曲的列表、显示信息和时间点，游戏/FMOD 事件逻辑负责 DJ、Stinger、转场音等触发时机。**

所以不同替换目标需要改的东西不一样：

```text
替换普通歌曲：
需要改 R*_Tracks_*.assets.bank + RadioInfo_*.xml

替换主菜单音乐：
主要改 GLB_RadioPressStart.assets.bank

替换 Stinger / Jingle：
主要改 R*_Stingers_语言.assets.bank，一般不改 RadioInfo XML

替换 DJ 语音：
主要改 VO_DJ_*_语言.assets.bank，一般不改 RadioInfo XML

修改歌曲循环、Drop、结束点：
必须改 RadioInfo_*.xml 里的 marker / SampleLength / End
```

---

## 1. 游戏电台系统的三层结构

### 1.1 音频 Bank 层

主要路径：

```text
media/Audio/FMODBanks/*.bank
media/Audio/FMODBanks/*.assets.bank
```

开发者扫描显示，绝大多数 bank 是成对出现的：

```text
XXX.bank
XXX.assets.bank
```

实际替换时重点关注：

```text
XXX.assets.bank
```

一般可以这样理解：

```text
XXX.bank          更像元数据 / 事件结构 / 索引侧
XXX.assets.bank   更像实际音频采样数据侧
```

绝大多数可提取音频都在 `.assets.bank` 中。替换声音时，通常替换的是 `.assets.bank` 里的某个 `sound_*.wav`。

---

### 1.2 RadioInfo XML 层

主要文件：

```text
media/Audio/RadioInfo_EN.xml
media/Audio/RadioInfo_CN.xml
media/Audio/RadioInfo_JP.xml
...
```

RadioInfo XML 主要负责普通电台歌曲相关内容：

```text
1. 电台有哪些歌曲
2. 每首歌显示什么标题 / 歌手
3. 每首歌的 SampleLength / End
4. TrackDrop / PostDrop
5. TrackLoopStart / TrackLoopEnd
6. PostLoopStart / PostLoopEnd 或 PostRaceLoopStart / PostRaceLoopEnd
7. FreeRoam / Event / Streamer Mode 播放列表
```

普通歌曲替换如果只改 bank，不改 XML，可能出现：

```text
歌曲标题不对
歌曲提前结束
Loop 点错位
放完一首就停
切歌时机不对
进比赛 / 冲线时切入点不对
```

---

### 1.3 游戏 / FMOD 事件逻辑层

这一层目前不能完全直接编辑。它更像运行时调度系统，负责：

```text
什么时候插入 DJ
什么时候播放 Stinger
什么时候比赛开始切歌
什么时候冲线后进入 PostLoop
菜单 / 漫游 / Streamer Mode 使用哪套播放逻辑
```

这也是为什么：

```text
歌曲替换可以比较稳定；
DJ / Stinger 可以替换声音本体；
但 DJ / Stinger 的触发时机目前不一定能直接控制。
```

---

## 2. Bank 文件主要类别与用途

开发者模式扫描结果表明，游戏音频目录中 bank 数量非常多，但对电台替换真正重要的主要是以下几类。

---

### 2.1 普通歌曲 Bank：`R*_Tracks_*.assets.bank`

典型文件：

```text
R1_Tracks_CU1.assets.bank
R1_Tracks_Disk.assets.bank
R2_Tracks_CU1.assets.bank
R2_Tracks_Disk.assets.bank
R3_Tracks_CU1.assets.bank
R4_Tracks_CU1.assets.bank
R5_Tracks_Disk.assets.bank
R6_Tracks_CU1.assets.bank
R7_Tracks_CU1.assets.bank
R8_Tracks_CU1.assets.bank
R9_Tracks_CU1.assets.bank
R10_Tracks_Disk.assets.bank
```

用途：

```text
存放普通电台歌曲
每个 sound_x.wav 通常是一首歌
时长一般为几十秒到数分钟
```

替换普通歌曲时，核心就是：

```text
找到 XML 中的歌曲槽位
找到它对应的 R*_Tracks_*.assets.bank
替换对应 sound_x.wav
重建 bank
同步修改 RadioInfo XML
```

---

### 2.2 主菜单音乐 Bank：`GLB_RadioPressStart.assets.bank`

已确认主菜单 / Press Start 音乐在：

```text
GLB_RadioPressStart.assets.bank
```

特点：

```text
里面只有 1 个长音频
不属于普通电台歌曲槽位
```

替换流程：

```text
定位 GLB_RadioPressStart.assets.bank
Extract 唯一音频
替换为用户选择的音乐
Rebuild bank
```

通常不需要走普通电台 RadioInfo XML 流程。

---

### 2.3 全局 / 特殊电台 Bank：`GLB_Radio_3D.assets.bank`

开发者扫描显示：

```text
GLB_Radio_3D.assets.bank
包含多个长音频
可能和全局 radio、3D radio、Streamer Mode 或特殊场景有关
```

注意：

```text
它不是普通 R1-R9 电台的标准歌曲 bank
但在 Streamer Mode 候选映射中出现较多
```

所以后续处理 Streamer Mode、漫游模式“放完一首就停”等问题时，不能忽略它。

---

### 2.4 Stinger / Jingle Bank：`R*_Stingers_语言.assets.bank`

典型文件：

```text
R1_Stingers_EN.assets.bank
R1_Stingers_CN.assets.bank
R2_Stingers_EN.assets.bank
...
```

用途：

```text
电台片头
Station ID
短转场
Jingle
广告 / 包装感音效
```

一般结构：

```text
每个可提取 Stinger bank 通常有若干短音频
每条多为几秒到十几秒
```

替换 Stinger 时通常只需要：

```text
替换对应 R*_Stingers_语言.assets.bank 里的 sound_x.wav
保持数量和顺序不变
尽量保持时长接近原始音频
```

通常不需要改 RadioInfo XML。

重点注意：

```text
Stinger 的触发时机大概率由 FMOD event / 游戏逻辑控制，
不是由普通歌曲 XML 控制。
```

也就是说，我们可以替换“播出来的声音”，但不一定能直接决定“什么时候播”。

---

### 2.5 DJ 语音 Bank：`VO_DJ_*_语言.assets.bank`

典型文件：

```text
VO_DJ_01_EN.assets.bank
VO_DJ_01_CN.assets.bank
VO_DJ_02_EN.assets.bank
...
```

用途：

```text
DJ 说话
电台主持人介绍
歌曲间评论
比赛 / 活动相关播报
```

结构特点：

```text
每个 VO_DJ bank 通常有大量短语音
每条语音可能对应一个 DJ 句子或某个场景反应
不同语言有不同 bank
```

替换 DJ 的核心规则：

```text
1. 替换 VO_DJ_*_语言.assets.bank
2. 保持 sound 数量不变
3. 保持 sound 顺序不变
4. 新语音时长尽量接近原语音
5. 不要指望 RadioInfo XML 能控制 DJ 触发时机
```

风险：

```text
新 DJ 台词太长：可能和歌曲 / Stinger 重叠
新 DJ 台词太短：可能出现尴尬空白
替换错 sound index：DJ 会在错误场景说错误台词
```

---

### 2.6 Horn / SFX Bank：不是普通电台歌曲

例如：

```text
Horn_SFX_*.assets.bank
Horn_Musical_*.assets.bank
```

这些在匹配表中有时会因为名字相似被误判成歌曲候选，例如：

```text
Speed
Rewind
Falling
777
```

但它们本质上不是普通电台歌曲。

工具后续必须避免：

```text
只因为名字匹配，就把歌曲映射到 Horn / SFX bank
```

判断普通歌曲候选时，应优先要求：

```text
candidate_bank_role = radio_tracks
时长接近
bank 名符合 R*_Tracks_*.assets
```

---

## 3. 电台与歌曲 Bank 的大致对应关系

根据开发者测试结果，目前普通电台大致对应如下：

```text
Horizon Pulse
→ R1_Tracks_CU1.assets.bank
→ R1_Tracks_Disk.assets.bank

Horizon Bass Arena
→ R2_Tracks_CU1.assets.bank
→ R2_Tracks_Disk.assets.bank

Horizon Block Party
→ R3_Tracks_CU1.assets.bank

Horizon XS
→ R4_Tracks_CU1.assets.bank

Hospital Records
→ R5_Tracks_Disk.assets.bank

Gacha City Radio
→ R6_Tracks_CU1.assets.bank
→ 少量可能关联 R5_Tracks_Disk.assets.bank

Sub Pop Records
→ R7_Tracks_CU1.assets.bank

Horizon Wave
→ R8_Tracks_CU1.assets.bank
→ 少量可能关联 R3_Tracks_CU1.assets.bank

Horizon Opus
→ R9_Tracks_CU1.assets.bank

Streamer Mode
→ 不是单一 bank
→ 会复用多个电台歌曲
→ 还可能涉及 GLB_Radio_3D.assets.bank
```

重点：

**不能只靠 R 编号硬猜。**

例如：

```text
Gacha City Radio 主要是 R6，但少量候选落到 R5。
Horizon Wave 主要是 R8，但少量候选落到 R3。
Streamer Mode 更复杂，是多个电台 / 全局内容的组合。
```

所以工具不应该简单写死：

```text
R6 电台 = 只改 R6_Tracks_CU1
```

而应该结合：

```text
RadioInfo XML
xml_to_bank_mapping.csv
fmod_all_bank_audio_inventory.csv
实际 sample_length
bank_role
```

来判断。

---

## 4. 歌曲 Marker 字段含义

普通歌曲 XML 里最重要的是这些字段。

---

### 4.1 `SampleLength` / `End`

含义：

```text
这首歌最终 prepared WAV 的总 sample 数
```

关键规则：

```text
必须以最终写入 bank 的 WAV 为准
不能用原始 MP3 / FLAC 的长度
不能用转换前 WAV 的长度
```

如果 `SampleLength` / `End` 错了，可能出现：

```text
歌曲提前结束
放完一首就停
切歌延迟
Loop 点错位
游戏认为歌曲已经播完
```

---

### 4.2 `TrackDrop`

含义推断：

```text
正常播放、比赛开始、切入歌曲时，适合进入主段落的位置
```

可以理解为：

```text
“从这里开始更有冲击力”
```

通常适合设置在副歌、drop、主旋律开始处。

---

### 4.3 `PostDrop`

含义推断：

```text
赛后、冲线后、特殊过渡状态适合切入的位置
```

可以理解为：

```text
“比赛结束后播的情绪段落”
```

---

### 4.4 `TrackLoopStart` / `TrackLoopEnd`

含义：

```text
普通播放或漫游状态下的主循环区间
```

要求：

```text
TrackLoopStart < TrackLoopEnd
TrackLoopEnd <= SampleLength
```

如果游戏需要循环当前歌曲，可能会在这个区间内循环。

---

### 4.5 `PostLoopStart` / `PostLoopEnd`

含义：

```text
赛后 / 过渡状态使用的循环区间
```

有些 XML 或工具里可能叫：

```text
PostRaceLoopStart
PostRaceLoopEnd
```

需要兼容这两种命名。

---

## 5. Marker 最关键规则：必须使用最终 prepared WAV 坐标

这是当前最容易出 bug 的地方。

用户导入的音频可能是：

```text
MP3
FLAC
96kHz WAV
44.1kHz WAV
单声道 WAV
高采样率 WAV
```

工具最终为了写入 bank，可能会处理成：

```text
48kHz
Stereo
PCM WAV
响度标准化后的 WAV
裁剪 / 补齐后的 WAV
```

这时：

```text
原始音频 sample 数 ≠ 最终 prepared WAV sample 数
```

所以不能直接把原始 marker 写进 XML。

正确逻辑：

```text
用户在 UI 中设置 marker
↓
转换 / 标准化音频
↓
得到最终 prepared WAV
↓
读取 prepared WAV 的 sample_rate 和 total_samples
↓
把 marker 转换到 prepared WAV 的 sample 坐标
↓
写 XML
```

如果 UI 里 marker 是秒：

```text
final_marker = round(marker_seconds * final_sample_rate)
```

如果 UI 里 marker 是原始 sample：

```text
scale = prepared_total_samples / source_total_samples
final_marker = round(source_marker * scale)
```

一句话：

**XML 写入的一切 SampleLength、End、TrackDrop、LoopStart、LoopEnd，都必须以最终 prepared WAV 为准。**

---

## 6. 替换普通歌曲的正确流程

### 6.1 需要修改

```text
1. 目标 R*_Tracks_*.assets.bank
2. 对应语言的 RadioInfo_*.xml
```

如果要多语言都正常显示，就要改多个：

```text
RadioInfo_EN.xml
RadioInfo_CN.xml
RadioInfo_JP.xml
...
```

### 6.2 推荐流程

```text
1. 选择目标语言 XML
2. 选择目标电台
3. 选择目标 slot
4. 根据 xml_to_bank_mapping 找到候选 bank 和 sound_x.wav
5. 准备替换音乐
6. 转换为最终 prepared WAV
   - 48kHz
   - Stereo
   - PCM WAV
   - 响度匹配
7. 读取 prepared WAV 的真实 sample 数
8. 重新计算 SampleLength / End / marker
9. 替换 bank 里的对应 sound_x.wav
10. Rebuild bank
11. Patch RadioInfo XML
12. 验证 XML 是否仍然 well-formed
13. 进游戏测试
```

### 6.3 注意事项

```text
不要只按 sound_name 全局替换
不要只按标题匹配
不要只按 R 编号猜 bank
不要用原始音频长度写 SampleLength
不要只改一个 XML 节点而忽略重复 alias
```

最安全的匹配 key：

```text
xml_file + station + slot_index + sound_name
```

不要只依赖：

```text
sound_name
```

因为测试发现：

```text
有些 sound_name 会在多个 station/context 中重复出现
有些重复 sound_name 甚至 sample_length 不一样
Streamer Mode 就是典型例子
```

---

## 7. 替换 Stinger / Jingle 的正确流程

### 7.1 目标文件

```text
R1_Stingers_EN.assets.bank
R2_Stingers_EN.assets.bank
...
R1_Stingers_CN.assets.bank
R2_Stingers_CN.assets.bank
...
```

### 7.2 一般不需要改

```text
RadioInfo_*.xml
```

### 7.3 替换流程

```text
1. 找到目标电台对应的 R*_Stingers_语言.assets.bank
2. Extract
3. 试听 sound_0.wav ~ sound_n.wav
4. 确认每个 sound 的用途
5. 用新 stinger 替换对应 sound
6. 尽量保持时长接近
7. Rebuild bank
8. 进游戏测试切台、歌曲间隙、比赛前后触发
```

### 7.4 注意事项

```text
Stinger 很可能按 index 触发
所以 sound 顺序不能乱
```

错误示例：

```text
sound_3 原本是 station ID
你换成了结尾 jingle
游戏仍然会在 station ID 场景播放 sound_3
结果语义错乱
```

---

## 8. 替换 VO_DJ 的正确流程

### 8.1 目标文件

```text
VO_DJ_01_EN.assets.bank
VO_DJ_02_EN.assets.bank
...
VO_DJ_01_CN.assets.bank
VO_DJ_02_CN.assets.bank
...
```

### 8.2 一般不需要改

```text
RadioInfo_*.xml
```

### 8.3 替换流程

```text
1. 找到目标语言和目标 DJ bank
2. Extract
3. 对每个 sound_x.wav 做试听和人工标注
4. 建立映射表：
   sound index → 原句内容 → 用途猜测 → 新句内容
5. 替换对应 sound
6. 保持数量不变
7. 保持顺序不变
8. 新语音时长尽量接近原语音
9. Rebuild bank
10. 进游戏按场景测试
```

### 8.4 最重要注意

DJ 替换不是“想说什么就换什么”。

因为目前不知道每条 DJ 语音的精确触发条件，所以安全策略是：

```text
旧 sound 是短介绍，新 sound 也做短介绍
旧 sound 是歌曲间过渡，新 sound 也做歌曲间过渡
旧 sound 10 秒，新 sound 最好也 8~12 秒
旧 sound 30 秒，新 sound 可以 25~35 秒
```

避免：

```text
用很长的 DJ 台词替换短句
用语义不相关的句子替换
打乱 sound index
删除某些 sound
增加 sound 数量
```

---

## 9. 替换普通 SFX 的正确流程

SFX bank 很多，例如 horn、UI、环境音等，一般与电台歌曲无关。

如果要替换：

```text
1. 用 fmod_all_bank_audio_inventory.csv 找目标 bank
2. 试听确认 sound_x.wav
3. 替换同 index 音频
4. 保持时长、声道、音量接近
5. 一般不改 RadioInfo XML
```

注意：

```text
不要让工具把 Horn_SFX 当成普通歌曲 bank
```

---

## 10. 替换主菜单音乐的正确流程

目标明确：

```text
GLB_RadioPressStart.assets.bank
```

特点：

```text
只有 1 个音乐
不属于普通电台歌曲槽位
```

流程：

```text
1. 用户选择主菜单替换音乐
2. 工具定位 GLB_RadioPressStart.assets.bank
3. Extract 唯一 sound
4. 替换为 prepared WAV
5. Rebuild bank
6. 覆盖游戏或生成 mod 包
```

注意：

```text
不需要用户手动选择 bank
不应该混入普通 RadioInfo XML 流程
```

---

## 11. Streamer Mode 为什么特殊

测试结果显示：

```text
Streamer Mode 不是一个简单的 R10 普通电台
它复用了多个电台的歌曲
还可能关联 GLB_Radio_3D.assets.bank
```

所以 Streamer Mode 不能简单理解为：

```text
R10 = R10_Tracks_Disk + R10_Stingers
```

更合理的理解是：

```text
Streamer Mode 是一个特殊播放列表 / 过滤模式
它引用其他电台或全局 radio 内容
```

这也解释了为什么：

```text
Streamer Mode 放完一首就停
Streamer Mode 替换后行为和普通电台不同
```

后续修复 Streamer Mode 要重点看：

```text
RadioInfo XML 里 Streamer Mode 的节点
这些节点是否引用了其他 station 的 sound_name
SampleLength / End 是否正确
是否有 GLB_Radio_3D 相关候选
是否存在重复 sound_name 但不同 sample_length
```

---

## 12. 开发者模式输出表如何使用

### 12.1 `all_bank_extract_statistics.csv`

用途：

```text
看每个 bank 是什么类型
是否 extract 成功
有多少 sound
时长大概是多少
```

适合第一步筛选。

常见 `bank_role`：

```text
radio_tracks       普通歌曲
press_start        主菜单音乐
glb_radio_3d       特殊全局 radio
dj_or_stinger_hint DJ 或 stinger 候选
```

---

### 12.2 `fmod_all_bank_audio_inventory.csv`

用途：

```text
查看每一个 sound_x.wav：
在哪个 bank
第几个 index
多长时间
采样率是多少
```

这是替换 DJ / Stinger / SFX 时最重要的表。

---

### 12.3 `target_all_station_soundnames.csv`

用途：

```text
查看 RadioInfo XML 中每个电台有哪些歌曲槽位
```

它代表 XML 层。

适合分析：

```text
某语言有哪些 station
每个 station 有多少 slot
每个 slot 的 sound_name
sample_length 是多少
```

---

### 12.4 `xml_to_bank_mapping.csv`

用途：

```text
把 XML 里的歌曲槽位映射到具体 bank/sound
```

替换普通歌曲时最重要。

重点看：

```text
candidate_rank
candidate_bank
candidate_sound_file
candidate_frames
length_diff
confidence
candidate_bank_role
```

比较稳的判断条件：

```text
candidate_bank_role = radio_tracks
length_diff 接近 0
candidate_duration_sec 是歌曲长度
candidate_bank 是 R*_Tracks_*.assets
```

注意：

```text
text_hint 有时会误匹配到 SFX
length_candidate 更可信，但也要看 bank_role
```

---

### 12.5 `music_bank_inventory.csv`

用途：

```text
快速查看哪些 bank 是长音乐 bank
```

适合找：

```text
普通歌曲 bank
主菜单音乐 bank
特殊长音频 bank
```

---

### 12.6 `missing_track_candidate_shortlist.csv`

用途：

```text
分析隐藏槽位、alias、重复 ID、手动复核候选
```

特别适合解释：

```text
为什么下拉框显示的槽位数量和实际可替换槽位数量不一致
```

因为有些 XML 行可能是：

```text
内部 alias
_ID 行
Streamer Mode 复用项
不是普通可替换歌曲
```

---

## 13. 想“完美替换”必须遵守的规则

### 规则 1：普通歌曲不能只替换音频，还要同步 XML

普通歌曲必须同时处理：

```text
Bank 音频替换
XML metadata 修改
XML SampleLength 修改
XML marker 修改
```

---

### 规则 2：所有 marker 以最终 prepared WAV 为准

重点：

```text
不是原始 MP3
不是原始 FLAC
不是用户手动转的中间 WAV
而是最终写入 bank 的 prepared WAV
```

---

### 规则 3：保持 sound 数量和顺序

对 bank 来说，index 很重要：

```text
sound_0
sound_1
sound_2
...
```

不能随便删、加、排序。

尤其是：

```text
DJ
Stinger
SFX
```

更应该严格保持 index。

---

### 规则 4：替换 DJ / Stinger 时，优先保持时长接近

因为触发逻辑不在普通 XML 里。

```text
你只能安全地替换声音本体
不能完全控制它什么时候播
```

所以要尽量：

```text
同类替换
同长度替换
同语义替换
```

---

### 规则 5：每种语言要单独处理

RadioInfo 是按语言分开的：

```text
RadioInfo_EN.xml
RadioInfo_CN.xml
RadioInfo_JP.xml
...
```

DJ / Stinger 也是按语言分开的：

```text
VO_DJ_01_EN.assets.bank
VO_DJ_01_CN.assets.bank

R1_Stingers_EN.assets.bank
R1_Stingers_CN.assets.bank
```

所以多语言 mod 不能只替换 EN。

---

### 规则 6：不要叠加多个电台 mod

如果另一个 mod 已经改过 RadioInfo XML，可能导致：

```text
XML malformed
槽位错乱
SampleLength 不一致
marker 不一致
工具无法加载 XML
```

最安全做法：

```text
从原始文件开始
一次性合并所有修改
生成一个最终包
```

---

## 14. 后续工具开发建议

### P0：普通歌曲完美替换

必须优先修：

```text
Export marker
Import marker
prepared WAV 坐标
SampleLength / End
Loop marker
播放器波形和 marker 可视化
```

---

### P1：普通歌曲替换验证器

生成包前自动检查：

```text
XML 是否合法
SampleLength 是否等于 prepared WAV samples
LoopStart < LoopEnd
LoopEnd <= SampleLength
candidate_bank_role 是否为 radio_tracks
是否误匹配到 Horn/SFX
是否有重复 sound_name 未同步
```

---

### P2：Stinger 替换模式

提供开发者功能：

```text
选择 R*_Stingers_语言.assets.bank
显示所有 sound
试听
替换
保持 index
生成替换包
```

不承诺修改触发时机。

---

### P3：VO_DJ 替换模式

提供开发者功能：

```text
选择 VO_DJ_*_语言.assets.bank
批量试听 / 标注
导出 DJ mapping 表
替换指定 sound index
生成包
```

不承诺修改触发条件。

---

### P4：Streamer Mode 专项修复

重点分析：

```text
Streamer Mode XML 节点
重复 sound_name
GLB_Radio_3D.assets
SampleLength / End
播放队列
```

目标是解决：

```text
放完一首就停
替换后队列不连续
```

---

## 15. 最终总结

要实现“完美替换”，核心不是简单地把一个 WAV 塞进 bank。

正确理解应该是：

```text
普通歌曲 = Tracks bank 音频 + RadioInfo XML 时间数据
DJ = VO_DJ bank 音频 + 游戏事件触发
Stinger = R*_Stingers bank 音频 + 游戏事件触发
主菜单音乐 = GLB_RadioPressStart 唯一音频
Streamer Mode = 特殊复合播放列表，不是普通 R10 电台
```

最重要的工程原则是：

```text
1. 替换声音时保持 bank 的 sound index 不变
2. 替换歌曲时必须同步 XML
3. XML 的 SampleLength / End / marker 必须基于最终 prepared WAV
4. DJ 和 Stinger 可以替换声音，但触发时机目前不要承诺可控
5. 多语言、多电台、Streamer Mode 都不能靠 sound_name 简单全局替换
```

只要后续工具严格围绕这些规则设计，就能从“能替换”逐步接近“稳定、可预测、接近原生体验的完美替换”。
